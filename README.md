#Socket Server

Servidor para implementar un api rest con sockets. 

Reconstruir modulos de node
`````
npm install 
`````

Generar el dist
`````
tsc -w
`````

Iniciar el servidor
`````
npm run iniciar
`````

